const env = {

    // Max width and height of the canvas
    MAX_WIDTH: 960 * 1.5,
    MAX_HEIGHT: 540 * 1.5,
    
    // Min width and height of the canvas
    MIN_WIDTH: 480,
    MIN_HEIGHT: 270,

    // Scale of the canvas
    SCALE: 2,

    // canvas offset
    OFFSET: {
        x: 18,
        y: 24
    },

    // Center of the canvas
    CENTER: {
        x: 0,
        y: 0
    },

    CANVAS: null,
    CTX: null,
    
    MAP: {

        // Offset 
        OFFSET: {
            x: 0,
            y: 0
        },

    },


}